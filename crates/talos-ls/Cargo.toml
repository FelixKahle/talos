[package]
name = "talos_ls"
version = "0.1.0"
edition.workspace = true

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]
talos_core = { path = "../talos-core" }
talos_model = { path = "../talos-model" }
num-traits = "0.2.19"
rand = "0.10.0"

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "mutation_cycle_bench"
harness = false
path = "benches/mutation_cycle_bench.rs"

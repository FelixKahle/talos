[package]
name = "talos_model"
version = "0.1.0"
edition.workspace = true

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]
talos_core = { path = "../talos-core" }
num-traits = "0.2.19"
